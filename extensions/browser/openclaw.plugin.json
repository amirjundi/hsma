{
  "id": "browser",
  "cliCommands": [
    {
      "name": "browser",
      "description": "Manage OpenClaw's dedicated browser (Chrome/Chromium)",
      "hasSubcommands": true
    }
  ],
  "_hsmaNote": "Disabled by default in HSMA. Collection uses Camoufox, a hardened Firefox, because this plugin drives Chromium over CDP with no anti-detection and CDP is exactly what social platforms fingerprint. Leaving it on also made `browser --help` hang past 120s, which failed the write-cli-startup-metadata build step. Re-enable with plugins.entries.browser.enabled=true.",
  "enabledByDefault": false,
  "activation": {
    "onStartup": true,
    "onConfigPaths": ["browser"]
  },
  "contracts": {
    "tools": ["browser"]
  },
  "commandAliases": [{ "name": "browser" }],
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
